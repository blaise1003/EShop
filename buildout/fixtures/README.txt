Demo images usage
=================

After you import demo fixures, you must import demo images in "static" directory.

1. move images zip file in "static" directory:

    $ mv demo_images.zip ../static/images
    
2. move to static/images directory:

    $ cd ../static/images

3. unzip images file:

    $ unzip demo_images.zip